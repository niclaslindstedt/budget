---
type: Fixed
---

Tapping outside the entry-type dropdown inside a modal (Edit row,
Transaction, Complex entry, Edit-many, …) now only closes the
dropdown instead of also focusing the input or hitting the button
beneath the tap. Matches how the same dropdown already behaves inside
a sheet row.
