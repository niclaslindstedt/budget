---
type: Changed
---

The entry-type picker now labels the boundary between the types you actually
use and the ones you haven't touched yet — a small "Most used" / "Unused"
divider replaces the silent transition between the two groups so the list
reads as two sections instead of one long scroll.
