---
type: Changed
---

The budget sheet now renders as one continuous table — month names
sit inline as section headers and the column row sticks to the top
across every month, matching the read-only viewer and the accounts
transfer log. The per-month bordered cards are gone.
