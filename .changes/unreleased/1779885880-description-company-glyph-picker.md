---
type: Added
---

Budget rows tagged to a company now show a small building glyph
before the description text on both desktop and mobile so the
merchant tag is visible at a glance even when the description is
overridden. The description popover also exposes an inline company
picker — pick a merchant, change the existing one, or create a new
one straight from the description reveal without opening the full
edit-row modal.
