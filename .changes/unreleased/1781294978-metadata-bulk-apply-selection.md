---
type: Changed
title: Choose which similar entries get labelled
---

Checking "Also apply to N similar entries" in Metadata mode now expands a list of the matched entries, all ticked by default, so you can untick the ones that shouldn't be updated.
