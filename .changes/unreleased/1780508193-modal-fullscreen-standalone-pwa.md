---
type: Fixed
---

Fullscreen modals (editing a mortgage, a property, a budget row, and the like) now fill the whole screen in the installed iOS home-screen app instead of stopping partway down with a dead band below the buttons.
