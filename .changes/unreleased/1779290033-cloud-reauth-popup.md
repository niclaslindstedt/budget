---
type: Fixed
---

When a cloud sync session expires (most often Google Drive, whose
access tokens last about an hour), the cloud sync panel now opens
automatically with a Reconnect button so you can resume in one tap
instead of being stuck on a generic "Sync failed" message that just
retried the same expired token. A new Storage setting lets you turn
the auto-open off and reconnect from the sync status icon yourself.
