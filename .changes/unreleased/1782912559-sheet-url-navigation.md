---
type: Added
title: Sheet URLs
---

Every sheet now has its own address — `/budget`, `/budget-2`, `/salary`, … — so the address bar follows the sheet you're on, the Back button walks your sheet history, and a shared link opens the first sheet of that type (or offers to create one when you don't have it yet).
