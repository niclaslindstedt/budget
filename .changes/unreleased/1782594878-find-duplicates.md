---
type: Added
title: Find duplicates
---

The Accounts sheet's ⋯ menu now has **Find duplicates**, which scans every account's bank history for the same transaction imported into more than one account, suggests which account it belongs to (the copy whose balance doesn't reconcile is flagged as the likely wrong import), and lets you accept all the suggestions in one click to remove the stray copies.
