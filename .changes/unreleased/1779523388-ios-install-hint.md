---
type: Added
---

iPhone and iPad users opening Budget in Safari now see a small,
dismissable hint at the bottom of the screen pointing at the Share
icon and explaining how to add Budget to the home screen. iOS Safari
doesn't expose the install API that Android Chrome does, so this
guided cue replaces the missing browser prompt; it stays hidden when
Budget is already running from the home screen and never returns once
dismissed.
