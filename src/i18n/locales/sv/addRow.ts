import type { AddRowCatalog } from "../en/addRow";

const addRow: AddRowCatalog = {
  button: "Lägg till rad",
  ariaLabel:
    "Lägg till rad (håll inne för återkommande eller kategoriserad post)",
  longPressHint: "Håll inne för fler val",
};

export default addRow;
