# Changelog

All notable changes to this project are documented in this file.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

This file is regenerated by the release pipeline from changeset
fragments under `.changes/unreleased/` (see
`scripts/release/collate-changelog.mjs`). Do not edit it by hand
outside release commits — add a fragment to `.changes/unreleased/`
instead. See `AGENTS.md` for the format and the rules about when to
write a fragment.

## [Unreleased]

## [1.6.0] - 2026-06-29

### Added

- **Last activity column** — The Accounts and Savings tables now show the date of each account's most recent imported transaction, colour-coded from green to red so stale accounts stand out at a glance.
- **Homepage** — A new homepage at **/home** introduces the app, describes what it tracks, and explains why it asks for optional access to your own Dropbox or Google Drive — readable without signing in.
- **Dropdown type-ahead** — Type a few letters in any of the type, category, company, item, tag, or employer dropdowns to jump straight to the first matching entry — the matched characters are highlighted, and pausing for a few seconds clears the search so you can start over. Press Enter to save a new company without reaching for the Create button.
- **Suggested companies by merchant** — Tagging a transaction with a company now remembers the merchant, so the next time the same merchant turns up its company is offered as a one-tap suggestion at the top of the company picker — and the guesses get smarter the more you tag.
- **Association loans and interest on the value chart** — Record a property's share of a housing association's debt (per square metre plus its interest rate, as in the årsredovisning), and toggle the Visualize-value chart to deduct the interest you've paid — your own mortgages, and optionally that hidden association interest from the monthly fee.
- **Ignore an entry for statistics** — A new entry "…" menu action keeps a transaction in the ledger and running balance but excludes it from the spending charts — for real charges that aren't your own spending, like paying for someone else. Ignored entries are tinted in the ledger so they're easy to spot.
- **Find duplicates** — The Accounts sheet's ⋯ menu now has **Find duplicates**, which scans every account's bank history for the same transaction — same date, description, amount, and running balance — imported into more than one account, pre-selects the account it belongs to (following the full running-balance chain, transfers included, so the genuine owner is picked), and lets you accept all the suggestions in one click — or set one account as the owner for every find at once when a whole statement went to the wrong place — to remove the stray copies; tap any match to see the surrounding bank history, where each matched balance shows a green checkmark when it sits cleanly on that account's running total or a red warning when it doesn't. When a whole statement landed in the wrong account, tick **remove the rest of that import** to drop the entire mis-imported batch at once, or **Ignore** a legitimate recurring charge so it's never flagged again. Duplicates are also caught **as you import**: if rows in the file you're importing already exist in another account, a picker opens automatically so you choose which account truly owns them and every copy is consolidated there, before the duplicates pile up — and when one account's history over the statement's date range is exactly those rows and nothing else, it's pre-selected as the owner. Resolving a duplicate also carries any category, company, tags, splits or receipt you'd added on the losing copy onto the surviving one, so time spent categorising on the wrong account isn't wasted.
- **Import overlap check** — Importing a bank statement into an account that already has history for that period now asks you to confirm first — a statement belongs to one account, so this catches importing into the wrong one. A few days of overlap (late-posting card charges from the previous statement) is allowed without prompting.

### Changed

- **Instant load for cloud budgets** — Cloud-backed budgets now show their locally cached copy the moment you open the app and refresh from the cloud in the background, instead of blanking until the cloud responds — the on-device cache is enabled by default and can be turned off in Settings.
- **Move a recurring date, move the whole run** — Changing a recurring entry's date with "This entry and all future" selected now slides every upcoming occurrence by the same number of days, instead of relabelling only that one row.
- **Finished-transaction highlight** — The Done column now grades your imported transactions: a grey check while one still needs work, and a green check plus a green row once it carries a type and either a company or "omit company" — so you can scan a statement for what's left to categorise.
- **Favicon** — The browser tab icon is now just the gold `# Changelog

All notable changes to this project are documented in this file.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

This file is regenerated by the release pipeline from changeset
fragments under `.changes/unreleased/` (see
`scripts/release/collate-changelog.mjs`). Do not edit it by hand
outside release commits — add a fragment to `.changes/unreleased/`
instead. See `AGENTS.md` for the format and the rules about when to
write a fragment.

 on a transparent background, with no dark tile behind it.
- **Faster, smaller sync** — Your budget is now gzip-compressed before it's stored or synced, shrinking it to a fraction of its previous size so cloud uploads and local saves are noticeably faster — existing budgets are converted automatically the first time they save.
- **Company per split** — The Split entry modal now lets each split carry its own company, so a credit-card bill or bankgiro can be broken into the individual purchases' real merchants.
- **Streamlined import-matching modal** — The "Match imported entries" modal now lays matches and orphan predictions out as cards in the cross-account-transfer style and adds a "Check all" toggle, so confirming a long import no longer means ticking every row by hand.
- **Date and amount on import renames** — The "Review suggested renames" step now shows each transaction's date and amount so you can verify a learned rename before applying it.

### Fixed

- **Learn more scroll position** — Following a "Learn more" link in the changelog now opens the feature doc at the top, and pressing Back returns you to where you were in the list.
- **Balance monitor button placement** — The add-monitor button on a scenarios sheet now sits next to the "Balance Monitors" heading instead of drifting to the far right edge on desktop.
- **New company selection in label-by-pattern** — Creating a company from the label-by-pattern dialog now keeps it selected in the company dropdown instead of clearing the form.
- **Suggested type pills** — Suggested types in the type picker no longer show a stray outline around each pill.
- **Sync conflict copies** — A cloud sync conflict no longer shows both the local and cloud copies as empty — the resolution dialog now reads the real entry counts so you can tell the two apart before picking a side.
- **Same-day rows in balance order** — Imported transactions that fall on the same day are now stored and shown in the order the running balance actually follows, instead of whatever order the bank's file happened to list them — so an account's history reads as one clean chain and same-day balances line up.

## [1.5.0] - 2026-06-17

### Added

- **Amount calculator** — Tap the calculator beside a split amount and type a sum like `100 + 30 + 50` to drop the total straight into the field.
- **Item appreciation** — Record dated value snapshots for an item from its "…" menu on the Items sheet, so things that appreciate — art, sculptures, collectibles — climb across the net-worth graph instead of sitting flat at their purchase price.
- **Copy bank text** — The description popover lets you copy the original bank statement text in one tap on history rows — beside the input before you've given the row your own description, and on the bank-memo line once you have.
- **Import values from a file** — Every "Update value / balance" modal can now import a batch of dated values from a CSV or Excel file — pick the date and value columns on a live preview of the sheet and import years of history at once. [Learn more](feature:batch-value-import)
- **Update download progress** — The **budget** wordmark in the header now fills with gold from the bottom — like a glass of water — while a new version downloads, and the reload prompt appears once it is full.
- **Amortisation-plan changes** — A mortgage's amortisation plan can now change over time — add the dates it
  stepped (e.g. 3% → 2%) like interest-rate changes, and each payment's
  amortisation follows the plan that was in effect that month, with the
  difference landing on interest.
- **Cover transfers** — Reimburse expenses charged to the wrong account with a "cover" transfer from a savings or spending account — it totals the selected imported transactions, generates a short bank reference message, links back to what it covered, is detected automatically on your next import, and moves the spending off the charged account onto the covering account's budget. [Learn more](feature:cover-transfers)
- **View full transfer descriptions** — Tap a transfer row's description in the budget ledger to open a read-only popover with the full bank description, even when the inline text is truncated.
- **Estimate range when promoting history** — Promoting a bank-history entry to a recurring series now offers the same exact-or-estimate amount control as other entry modals, so a varying bill can be promoted with a min/max range.
- **Suggested companies by type** — Set a row's type first and the description popover's company picker now lists that type's most-used companies up top, so you skip scrolling the whole library to reach the merchant you always use for it.
- **Entry info view** — A read-only info button on every budget and imported row (or a long-press / right-click of the row) opens a full breakdown of the entry — bank text, description, amount, type, company, tags, splits — with copy glyphs for each field and a "Copy all details" button.

### Changed

- **Choose which similar entries get labelled** — Checking "Also apply to N similar entries" in Metadata mode now expands a list of the matched entries, all ticked by default, so you can untick the ones that shouldn't be updated.
- **Net worth over time** — The Insights net-worth chart stacks each category as its own band — assets above a zero line, other loans below it, and a line tracing your total — with a property's mortgages folded into its band so the two figures that dwarf everything else move together. Tick a band off in the legend to drop it from the stack and rescale the chart, so the smaller balances become readable.
- **Type exact amounts and dates in search filters** — Click the from / to values above a search filter's amount or date slider to type an exact bound instead of dragging the thumb.
- **Wider mortgage match tolerance** — The "Find mortgage payments" match-tolerance slider now reaches ±200%, so months with a large rate-driven swing in the charge still count.
- **Mortgage auto-detection trusts your tags** — A charge you marked as the mortgage, or one whose description matches a payment you already recorded, is now auto-selected as a likely payment even if a month was slipped or missed — only amount-only guesses still require a clean monthly run.
- **Find mortgage payments across several accounts** — The "Find mortgage payments" walk now has an account dropdown — pre-set to the property's payment account, but you can select several at once, so a loan whose payments moved between accounts over time is still found in one pass.
- **Paid-off mortgages read as "Paid off"** — A fully-paid mortgage no longer shows a balance, interest, or amortisation per
  month on its card — it simply reads as paid off, with the recorded payments
  still a tap away in the payments view.
- **Yearly percent on the amortisation figure** — A mortgage card's amortisation per month now spells out the annual percent the
  plan is set in — e.g. "(2% / yr)" — on both the per-loan card (the plan's own
  percent) and the unified combined card (the blended rate across the loans).
- **Mortgage payments tag their transactions** — Accepting payments from Find mortgage payments now tags the bank transactions behind them with the Mortgage type and the property's lender, so they carry their metadata into the budget view and search without tagging by hand. [Learn more](feature:properties)
- **Tap a savings account to view its history** — Open a savings account's imported transaction history by tapping its row — the same viewer the accounts page uses — instead of the "View history" item in the swipe menu.
- **Wider description popover for long memos** — The budget description popover now grows toward the page edge to fit a long bank memo on one line before wrapping, instead of squeezing it into a narrow column with a scrollbar.

### Fixed

- **Omit company when splitting** — Splitting a bank entry in metadata mode now offers the same "Omit company" option as the single-entry form, so a part with no merchant can be marked as such.
- **Metadata mode save gating** — In metadata mode, setting only a type now consistently keeps Save disabled with the "pick a company — or Omit company — to save" hint, instead of letting you save a half-labelled entry that never left the queue.
- **Yearly amortisation label** — The mortgage editor now labels the "% of initial loan" amortisation as "Yearly amortisation", matching how the percentage is applied.
- **Keep company and type when editing a historic item** — Editing a bank-history entry that already has a company and type connected now opens with both pre-selected, instead of starting from blank pickers.
- **Net sale profit hidden for sold properties** — A sold property no longer offers the forward-looking Net sale profit estimator in its actions menu.
- **Mortgage payments slipped across a month boundary** — The "Find mortgage payments" walk no longer drops a payment when a weekend pushes it into the next month, where two charges then share a calendar month — charges at least two weeks apart now count as separate payments.
- **Mortgage finder no longer mixes up properties** — "Find mortgage payments" now matches charges on their exact bank description, so two properties paid under the same prefix (e.g. "Avibetalning" with different reference numbers) and similar amounts are no longer merged — one property's payments are never offered for another.
- **Find mortgage payments pre-checks fewer candidates** — Find mortgage payments no longer pre-checks every charge it surfaces when nothing stands out as highly probable — it pre-checks just the leading candidate, so the tail of weak one-off matches is left for you to opt in rather than added by default.
- **Mortgage payment split on sold properties** — Find mortgage payments now divides a combined charge across a sold property's
  loans using the balance each loan carried that month — reconstructed from the
  original loan amount — instead of the balance recorded today, which is zero
  once the property is sold.
- **Percent amortisation on the property's total loan** — A mortgage that amortises by an annual percent now takes that percent against
  the property's **combined** initial loan, not just its own — so a small
  amortising top-up alongside a large interest-only loan amortises (e.g.) 2% of
  the whole property's original debt, matching how the Swedish amortisation
  requirement is set.
- **Promoted recurring candidates keep every field** — Promoting a detected recurring candidate now mints rows that carry the company, tags, transfer flag, completed state, estimate min/max band, and formula you set in the confirm modal — previously these were silently dropped and only the description, amount, and type survived.
- **Label-by-pattern preview order** — The matching-entries preview now lists newest first, so the year heading appears only when the list crosses into an earlier year.
- **Apply a company or type change across a recurring series** — Changing the company or type on a recurring entry now offers to apply the change to every following occurrence, matching how editing the description already works.
- **Import matches show the row's real label** — The "Match imported entries" modal now labels each matched and orphan row the same way the budget table does — falling back to the company pill or type name when there's no typed description — instead of showing "(no label)".
- **History column alignment** — The bank-history viewer now keeps its date and description columns aligned across rows on mobile, instead of shifting when a date is shorter.
- **Cleaner balance column in landscape** — The balance column no longer shows a redundant +/- glyph that crowded the number in landscape — direction is conveyed by the value's colour.

## [1.4.0] - 2026-06-12

### Added

- **Insights sheet** — A new sheet type that rolls everything you track into your net worth — accounts, savings, items, and properties minus mortgages and loans — with a breakdown, a chart over time, and settings to exclude things or set an ownership share for anything co-owned. [Learn more](feature:insights)
- **Sold properties** — Record a sale date (and price) on a property you no longer own — it stays on the Properties page with all its history, but stops counting toward your net worth and mortgage-payment discovery from the day it was sold.
- **Investment sheet** — A new sheet type for tracking investments: a holdings table for funds, shares, gold, and crypto whose account type (ISK, KF, or depå) sets the tax, plus a private-stocks table that tracks your shares, average cost, and buy/sell trades — each with a net value after tax and a value-over-time chart. [Learn more](feature:investment)
- **Scenarios sheet** — A new sheet type for what-if modeling against a budget you already track: create scenarios that change amounts — typed in, or as live adjustments (+5 000, −500, ×2, +300 %) that keep tracking the underlying entry when it changes — with an optional sweep across a recurring entry's upcoming occurrences, drop expenses (recurring ones future-and-all in one gesture), or add one-off or recurring rows without touching the real budget, compare every scenario's projected balance 1 month to 2 years ahead in the Visualize scenarios chart, set monitor dates to see the projected balance on days that matter, and view a scenario's changes as a diff against the unaltered baseline. [Learn more](feature:scenarios)

## [1.3.0] - 2026-06-11

### Added

- **Loans sheet** — Track the money you owe — student loans, car loans, mortgages (link one or several of a property's mortgages from the Properties sheet as a single loan), private and personal loans — record the outstanding balance as of a date, and import payments from your bank transactions so the remaining balance follows along: mark one charge with the loan's type and the import suggests the rest by bank description and similar amount, optionally tags and renames them for you, and attaches matching charges automatically on future imports. **Visualize loans** in the sheet's … menu charts how each loan stacks into your total debt over time, with monthly payment bars, an estimated-interest break-out, time-range buttons (1Y / 2Y / 3Y / 5Y / all) that zoom past the flat years before your first transaction, a balance-change percentage for the window — green when you've paid debt down, red when it grew — and a salary-multiple mode that shows your debt as months of take-home pay using the paychecks on your Salary sheet. [Learn more](feature:loans)
- **Visualize spending** — The budget sheet's … menu gains a dashboard of where your money actually went (completed entries and imported bank history only): monthly bars stacked per category, a donut you can click to drill into a category's types, income against expenses month by month, and your top merchants, all over a 3M / 6M / 12M / All window. Give an owned item a **Lifetime** in the item editor and the dashboard's cogwheel option spreads its cost evenly across that lifetime — Swedish "avskrivning" — so big purchases stop spiking the month they happened.
- **Accelerated depreciation** — The item editor's depreciation box gains a **Steady / Accelerated** toggle: the accelerated model takes an instant drop the moment the item is no longer new (a car driven off the lot), a steeper first year, then a flatter yearly decline.

### Changed

- **Savings goal glyphs** — Savings accounts get their own glyph palette covering the things you save toward — vacations, clothing, kids' toys, electronics, a home, a car, pets, and more — instead of borrowing the bank-account icon set.
- **Faster item cataloguing** — Adding a line item now creates the item as you type its name — classified under the transaction's type, with a one-click subtype (pick one or type a new one inline) — and **Find items** stops suggesting purchases whose items already account for the full amount.

### Fixed

- **Saving after a large shrink** — The "Save anyway" button in the cloud-sync panel now pushes a paused save through when your budget shrinks a lot at once (such as after deleting an account), instead of doing nothing.
- **Account row menu** — Picking an action from an account row's "…" menu no longer also opens that account's history view behind the modal.
- **Reachable actions on narrow desktop** — The row action column no longer slides off-screen when a sheet table is too wide for the window on desktop — it collapses to a single ⋯ menu (carrying Edit and Delete) so the actions stay reachable without a swipe.
- **Android status-bar spacing** — In the installed app on Android, the page header and fullscreen dialog titles no longer sit flush against the status bar — they keep a small gap, matching how the app already clears the notch on iOS.
- Opening the preview site as `/preview` (without the trailing slash) no longer loads the production app when you have visited the main site first.
- The "reload to apply" update prompt now names the version it's upgrading to, instead of the version you're already running.

## [1.2.0] - 2026-06-09

### Added

- **Feature docs in What's new** — Large features in the "What's new" changelog now show a "Learn more" link that opens the full write-up inside the same window, with a back button to return.
- **Savings sheet** — Add a Savings sheet for tracking money you set aside: create savings accounts, record their balance over time — by hand or seeded automatically from an imported bank statement — let transfers from your everyday accounts into savings get matched automatically, and visualize the total set aside across the accounts you choose over time.
- **Table spacing** — Add a **Table spacing** control to the Custom theme (Settings → Appearance) — pick Compact, Comfortable, or Spacious to tighten or open up the padding in your budget ledger tables.

### Changed

- **Account details from imported statements** — Importing a bank statement now fills in the account's bank name alongside its clearing and account number when those fields are empty, and does so for savings accounts too — never overwriting details you set by hand.

### Fixed

- **Import history date range** — The import preview's date range now always shows the year on both bounds, so a statement that spans the turn of a year is no longer displayed as if it covered a single year.
- **Cutting history no longer strands collapsed transfers** — Cutting an account's history now brings back the other side of any collapsed transfer it removes, so re-importing the same statement detects the pair again instead of finding nothing.

## [1.1.0] - 2026-06-09

### Added

- **Properties sheet** — Track the homes and apartments you own — their value over time, mortgages, repairs and renovations, and documents — with automatic mortgage-payment detection, payoff tracking, value charts, and a net-sale-profit calculator. [Learn more](feature:properties)
- **Payslip pill** — When a payslip is attached, the salary row's gross amount becomes a tappable pill that opens the payslip in one tap.
- **Add payslip by hand** — A new Add payslip action on the Salary sheet records paychecks older than your imported bank history, no bank transaction required.
- **History filters** — The account history viewer gains a filter button to narrow imported entries by time range, amount band, or month span alongside the search.
- **Reorder sheets** — Drag your sheets into order in Settings → General to set the bottom-bar tab strip.
- **Favorite sheets** — Pin up to five sheets from a sheet's "…" menu as quick-switch glyph icons in the bottom bar.
- **Manage subtypes** — A Subtypes section in settings lists every subtype under its parent type, where you can rename or delete each one.
- **Repairs type** — A new Repairs type under Housing, for tracking what you spend fixing what's broken — separate from routine Home maintenance and larger Renovations.
- **Collapsible settings sections** — Tap a tall settings section's title (like Categories and types) to fold it into a slim bar, and tap again to expand it.
- **Excel theme** — A new Excel light theme under Appearance: a pure-white cell grid on Office-grey, the classic gridline grey, near-black text, and the Office accent set.

### Changed

- **Sheet switcher dropdown** — Sheet switching moved to a header dropdown listing every sheet (the active one checked, with a "New sheet" entry); the bottom bar now holds just the row actions.
- **Merged Mortgage types** — The Mortgage principal and Mortgage interest budget types are now a single Mortgage type, with entries tagged either one migrated automatically.
- **Instant cloud open** — Budgets on Dropbox or Google Drive now open instantly from a local copy and re-fetch in the background only when something actually changed.
- **Smarter Find salaries** — Find salaries now recognises the bank description behind the paychecks you've added and suggests other deposits under it within your payout-day window.
- **Tighter money columns** — Money columns now hug their widest value, staying narrow on phones and growing only when the numbers get large, with the header icon directly over the figures.
- **Custom theme starts from current** — Switching to the Custom theme now seeds its colours, radius, density, border width, and reduce-motion toggle from the theme already on screen.
- **Aligned colour pickers** — The Custom theme's colour swatches now line up in a tidy grid instead of drifting to wherever each caption pushed them.
- **Amount filter stays put** — The transaction-search amount filter now shows a short hint ("All results are X") instead of disappearing when every result shares one amount.
- **Items columns renamed** — The Items sheet now labels its money columns "Price" and "Value" (was "Bought for" / "Worth now"), with a Price icon on every screen size.
- **Items menu order** — The Items sheet's "…" title menu now lists Edit sheet first, matching every other sheet type.

### Fixed

- **Budget scroll flicker** — Fixed a jitter on the budget page when a month sat right at the edge of where its rows load in.
- **Offline payslips** — Viewing and removing a payslip now works on Dropbox and Google Drive when offline mode is enabled.
- **Settings title lines** — Settings section titles no longer show a faint line running through them on high-contrast themes like GitHub Light and Excel.

## [1.0.0] - 2026-06-02

### Added

- **Build identifier** — The running build version now appears next to "Show source" in the burger menu, shaped as `<version>[.<run>][-pre]`.
- **Donate button** — Settings now ends with a "Donate ♥" button that opens the maintainer's donate page, hidden on forks without their own URL.
- **Preset types** — Built-in preset types and categories for Swedish households, manageable in new **Categories** and **Types** Settings sections, with each type flaggable as income or expense.
- **Always abbreviate balance** — A new "Always abbreviate balance column" number-format setting compresses every running-balance row into compact figures for a uniform stack.
- **History reconciliation** — Bank-history imports now reconcile against predicted budget rows, prompting before merging close matches and carrying labels, types, and series links onto imported entries.
- **Amount formulas** — The complex create-entry modal can compute a row's amount from a formula like `endOfMonthBalance - 5000`, with colour-coded pills, cross-sheet `sheet()` references, and a help guide.
- **Range amount filter** — Pattern rules now support a signed "Range" amount filter with from/to band and +/- toggles, narrowing a rule to a specific price band.
- **Single-char wildcard** — Label-by-pattern rules now accept `?` as a single-character wildcard alongside `*`, so `?LA*` matches `BLA` but not `LAB`.
- **Dismiss all candidates** — The Recurring candidates panel now has a "Dismiss all" button that hides every detected candidate at once after confirmation, remembered like single dismissals.
- **Swedish & language picker** — Swedish translations with an English/Swedish flag picker in Settings, auto-detecting from browser language and translating built-in categories, types, and month names.
- **Currency presets** — Settings → Format now offers currency presets (SEK/NOK/DKK/ISK, EUR, GBP, CHF, USD/CAD) so you needn't spell out symbol, position, and spacing by hand.
- **Export download** — A download button next to the sheet title exports budget rows as CSV/Excel or accounts as JSON, via a modal that remembers your column and filter choices.
- **History row editing** — History rows in a budget view gain a pen-button edit modal and inline-editable description and type cells, with per-entry overrides that leave same-merchant entries untouched.
- **Split row** — A new "Split row" action carves an entry into separate same-date parts for mixed payments, with a "Revert split" button to restore the bank's original entry.
- **Hide transfers** — A "Hide transfers between accounts" toggle suppresses cross-account transactions and rows marked with the new **Mark as transfer** action, with hidden runs revealed on click.
- **Theme & font pickers** — Settings → Appearance adds Dark/Light/System/Custom themes and font choices, with custom controls for colour, radius, density, border, and motion previewing live.
- **Developer logs** — Developer mode reveals a Logs tab capturing log entries with level filtering, copy, and clear, stored on-device and available only in the `/preview/` build.
- **Offline cloud mode** — An opt-in "Work offline when the cloud is unreachable" toggle keeps a local copy of the Dropbox/Google Drive file, pushing edits on reconnect with conflict confirmation.
- **Undo & redo** — Undo/redo for every edit via ⌘Z/⌘⇧Z or bottom-bar buttons, plus an **Action history** modal listing each timestamped action with its subject so you can jump to a known-good point.
- **Candidate detail modal** — Clicking a Recurring candidates row now opens a modal listing the bank entries that drove detection, with date and signed amount per occurrence.
- **View mode** — A new eye-icon view-mode button opens a clean read-only viewer of a sheet's months and rows without editing chrome, anchored on today with paginated future-month reveals.
- **Transaction search** — A magnifier button on the bottom bar opens a cross-sheet transaction search, ranking matches by relevance with highlighting, configurable weights, sort menu, and multi-select bulk actions.
- **Storage warning** — A warning popup greets you on page load when the default browser backend crosses ~3 MB, nudging you toward a folder or cloud backend before writes fail silently.
- **Show source link** — Added a "Show source" link to the header menu that opens the project's GitHub repository.
- **Cut history** — A scissors button on each account row opens a "cut history" modal that drops imported entries and transfers before a chosen cutoff, previewing the count first.
- **PWA install** — Budget is now installable as a Progressive Web App with a standalone window, offline precaching, and a "reload to apply" prompt; production and `/preview/` install separately.
- **Achievements** — Every feature is now an unlockable achievement with playful names and tier-uniform points, surfaced by a header star and browsable in a fullscreen English/Swedish catalog.
- **Install banner** — An in-app banner prompts you to install Budget, pointing iOS Safari at the Share icon and offering Chromium browsers a native Install button, hidden once installed or dismissed.
- **Toast notifications** — Toasts confirm undo/redo, imports and exports, cloud connect/disconnect, deletions, and save errors, auto-dismissing or dismissable manually.
- **Shift days by** — The "Edit recurring entry/row" modals gain a "Shift days by" input that nudges every entry's date in the chosen scope by a signed number of days.
- **GoatCounter analytics** — The production site now loads GoatCounter, a privacy-friendly open-source page-view counter recording aggregated hits only, with no cookies and never on `/preview/` or dev.
- **Tap tab to top** — Tapping the active sheet tab in the bottom bar now scrolls the sheet back to the top.
- **Title click shortcut** — The "budget" header wordmark is now a configurable shortcut, picking between scroll-to-top, jump to current month, refresh, or opening a sheet.
- **Pull to refresh** — Pull down from the top to refresh the budget from storage, flushing unsaved edits to the cloud first and handing off to the sync-conflict modal if the remote moved.
- **Swipe between sheets** — In the installed PWA, swipe left or right on a neutral page area to switch sheets with wrap-around, while existing row-swipe and tablist gestures keep their surfaces.
- **Per-device settings** — Settings now split into common, mobile, and desktop scopes so display toggles can differ per device while locale, theme, and security stay shared.
- **Transaction order** — A Settings preference picks **Newest first** or **Oldest first**, applied across the budget sheet, viewer, transfer log, and history modal.
- **Show future entries** — A Display toggle hides future-dated budget rows behind a "Show 3 future months" divider, or renders a chosen number of upcoming fiscal months up front.
- **Label similar** — A row-menu action opens the pattern modal pre-filled from any budget row, auto-applies saved rules on manual entries, and re-labels existing rows, with a "Patterns" settings tab.
- **Pattern amount matching** — Pattern cards show an amount-match chip, an **Exact** mode pins a rule to a single amount, and up/down arrows reorder rule priority.
- **Prune cloud backups** — Each cloud backup gets a trash button so stale snapshots can be deleted.
- **Sign-in user picker** — The sign-in screen shows known users so a returning user picks their account in one click instead of typing.
- **Save pattern toggle** — "Label by pattern" gains a **Save pattern** checkbox so you can bulk-label existing matches and discard the pattern in one shot.
- **Stop recurring after a date** — Deleting a recurring entry can stop after a chosen date, useful for pausing a salary or membership without losing the rest of the series.
- **Rename memory** — Bank-history imports learn how you rename entries and offer a Review-suggested-renames dialog to reapply the same renames on future imports.
- **Find conflicts** — The budget `…` menu surfaces same-day, same-category, near-equal duplicate pairs and folds each into a single row, unlocking the _Doppelgänger_ achievement.
- **Metadata mode** — A focused walkthrough annotates imported entries one at a time — type, company, tags, description, transfer flag, splits — and can fan a label out to similar entries.
- **Month reconciliation flags** — The month footer marks fiscal months fully covered by bank history, with an action button to triage manual rows still inside the covered window.
- **Companies** — Tag budget entries with the merchant or organisation paid, optionally categorised, curated through pickers and **Settings → Companies** and wired into every edit flow.
- **Primary income** — Recurring series can be flagged as primary income so early-arriving pay is pushed into the next fiscal month, with manual push controls and the **Early Bird** achievement.
- **Primary income on imports** — The **Primary income** toggle reaches bank-imported entries, pushing future matching deposits into the next fiscal month and persisting across job changes.
- **Company glyph in ledger** — Company-tagged rows show a building glyph (crossed out when omitted) and the description popover exposes an inline company picker.
- **Original bank memo** — The description popover on overridden history rows shows the original bank memo read-only beneath the textarea, prefixed with a bank glyph.
- **Company picker in entry modals** — The New entry and Edit recurring entry modals gain a Company picker and a "Mark as transfer" checkbox.
- **Edit heartbeat** — Budget rows briefly pulse after an inline edit or when newly added, making it obvious which row just changed, respecting Reduce-motion.
- **Suggest types from company** — Picking a company auto-fills or suggests its type, with user-pinnable type priorities under **Settings → Companies**.
- **Search filters** — A filter popover in the search bar excludes transfers/history/unconfirmed entries and narrows by sheet, company, type, category, tag, amount, or date range.
- **Estimate ranges** — Budget entries can be an estimate range (min/estimate/max) instead of a single amount, so an imported transaction within the range still matches the entry.
- **Tags** — Colour-coded user labels group entries across categories, assignable on creation, in edit modals, in bulk, on history entries, or by wildcard pattern, and searchable.
- **Line items** — A row action ties part of a purchase to physical items you own, building a catalog with subtypes, depreciation, resale value, and optional receipts.
- **Items sheet** — A new sheet type lists everything you own with purchase and current value totals, receipt management, and a **Find items** scan of likely purchases.
- **Salary sheet** — A new sheet type tracks pay over time, with **Find salaries** detecting recurring paychecks, employer/role tagging, taxes, absence days, and payslip attachments.
- **Tap header to scroll up** — Tapping a modal's header scrolls its content back to the top, handy in the long budget viewer and account history modals.
- **Gross estimation** — Salary sheets estimate a paycheck's gross from its net deposit using real Swedish tax rules via a reusable per-sheet tax profile.
- **Attachment zoom and pan** — Payslips and receipts can be pinched, scrolled, or double-clicked to zoom and pan in the in-app viewer for both images and PDFs.

### Changed

- **Settings tabs** — The **Settings** modal now splits its sections into tabs with a desktop sidebar and mobile burger menu, keeping a stable height and a persistent footer of links.
- **Delete confirmation** — Deleting a sheet or account now opens a confirmation dialog so an accidental trash tap can be backed out before data is wiped.
- **Hide empty past months** — Past months with no entries are no longer shown above the current month, so a fresh budget opens with a single month.
- **Drive `budget/` folder** — The Google Drive backend now stores its file and backups in a dedicated `budget/` folder at the My Drive root, migrating existing files automatically.
- **Types within categories** — Every type now belongs to a category derived from its parent, replacing the category column with a type column and a category-first nested picker throughout.
- **Centered short modals** — Short modals without text inputs now appear as a centered card on mobile instead of filling the screen, with fullscreen reserved for keyboard-opening dialogs.
- **PolyForm relicense** — Relicensed from MIT to PolyForm Noncommercial 1.0.0, keeping the source public for audit while requiring a separate license for commercial reuse.
- **Wider color palette** — The shared color picker now offers sixteen saturated hues, adding pink, terracotta, amber, lime, teal, indigo, mauve, and earth-brown while still omitting gray.
- **No select-all on text inputs** — Plain text inputs in modals no longer select all on focus and instead carry an inline × clear button, while numeric inputs keep select-on-focus.
- **Migration notice on restore** — Restoring a backup taken with an older app version now tells you the snapshot was migrated to the current format.
- **Recurring backfills history** — "Make recurring" now also backfills the chosen type and description onto matching past bank-history entries, previewing the detected matches before confirming.
- **Better budget downloads** — XLSX exports are now structured Excel Tables with autofilter, Settings-aware formatting, and auto-fit columns, with Description moved next to Date in both CSV and XLSX.
- **Balance sign layout** — The balance column now renders with the same `+ / − $ 1 200` layout as the amount column, with a read-only sign glyph and slightly wider mobile columns.
- **Type/category create dialog** — Creating a new type or category from a row's "+" picker now opens a proper modal instead of cramming the form inside the floating dropdown.
- **Stable Settings height** — The Settings modal now pins to a stable 95% of the viewport height on desktop so the tallest tabs no longer push content past the card.
- **Upward-flipping dropdowns** — Dropdown menus now flip upward when there isn't enough room below the trigger, instead of being squeezed into a scrollable sliver near the bottom.
- **Side-by-side amount/type** — The Promote / complex-entry modal now lays out Amount and Type side by side to save vertical space, while the formula view still spans full width.
- **Same-date sort order** — Entries sharing a date now sort incomes first, then expenses grouped by category total, with descriptions breaking remaining ties alphabetically.
- **Promote backfill gating** — The "Promote history entry to recurring" modal now lists the past entries that would adopt the new label and gates backfill behind per-match checkboxes.
- **Row action `…` menu** — Only Edit and Delete stay on the swipe strip; every other row action moves into a new "…" dropdown, including a new **Copy** action for one-off rows.
- **Header reorganisation** — The page header now holds just the save indicator and a burger menu, with Undo/Redo/Select in a floating pill and account actions and danger-zone moves relocated.
- **Tighter mobile header** — Tightened the page header on mobile by reducing vertical padding, bottom margin, and control gaps, giving the sheet a sliver more room; desktop is unchanged.
- **VS Code theme presets** — Added Dracula, Monokai, GitHub Dark, GitHub Light, Solarized Light, and Quiet Light presets, splitting the Appearance picker into Mode and Variant rows.
- **Full-width sheet tab bar** — The sheet tabs are now a single solid full-width bar with horizontal scrolling and right-edge editor controls that hides on scroll-down and returns on scroll-up.
- **Unified Numbers panel** — Settings → Format now hosts a single **Numbers** panel that absorbs currency, with the preset picker, display toggles, and currency-wrapped preview chips together.
- **Fullscreen mobile history** — The Accounts page's account-history modal now fills the screen on mobile while staying a centered card on desktop, giving imported entries more vertical space.
- **Floating Today pill** — The header wordmark is no longer a scroll shortcut; a floating **Today** pill now jumps back to today's row when the current fiscal month is off-screen.
- **History modal redesign** — The account-history modal and budget viewer now match the budget view's TYPE column, month dividers, and resolved labels, and gain an in-place search bar.
- **Cell hover highlight** — Description and amount cells now highlight on hover, matching the other interactive columns.
- **Transfers rename** — Renamed the Accounts page's "Transactions" section to "Transfers", since the list shows cross-account moves rather than any single-account post.
- **Collapsed-transfer edit lock** — When a transaction was created by collapsing two history entries, its date, amount, accounts, and done flag are read-only, with a toggle to demote the pair.
- **Eye icon + history count** — The Accounts sheet's view-history button now uses the budget sheet's eye icon and shows each account's imported entry count beside it.
- **Directional import/export icons** — Settings → Import / export buttons now use ⬇ for Import and ⬆ for Export in matching order, and the per-account import button picks up the ⬇ icon.
- **Centered headers + actions column** — Centered every sheet column header and gave the row-actions strip its own "Actions" header, with the accounts table's row buttons on a fixed aligned grid.
- **Glyph on modal titles** — Every modal now leads its title with a small purpose glyph — pencil, calendar, magnifying glass, wallet, and so on — the way the Edit transaction modal already did.
- **First sheet named "Budget"** — A fresh install's first sheet is now named "Budget" instead of "Sheet 1", a more useful label that reads the same in English and Swedish.
- **Narrower mobile type column** — Mobile sheet rows now give the type column the same 40 px glyph track as the completed column, freeing space for the description and tightening the amount column.
- **Narrower mobile date column** — Mobile sheet rows now give the date column the same 40 px track as the type and completed columns, since it renders only a 1–2 digit day.
- **Changelog as modal** — "What's new" now opens the changelog as a modal instead of navigating to a `/changelog/` page, with a "Show all changes" link expanding to full history.
- **No mobile balance glyph** — The running-balance column no longer shows a `+`/`-` glyph on mobile, conveying direction by sign colour alone; the glyph stays on desktop.
- **Accessibility pass** — A sweeping accessibility pass adds arrow-key listbox navigation, proper ARIA tablists, focus trapping, `scope="col"` headers, landmarks, and WCAG-minimum hit areas.
- **Accounts tables restyled** — The Accounts sheet's Accounts and Transfers tables now follow the budget sheet's headers, density, and pilled entry-type chips, keeping the transfer column tight on mobile.
- **Accounts swipe affordance** — The **Accounts** table now mirrors the budget sheet's swipe-to-reveal pen/trash/more actions, with a transaction-count column and a grouped More menu.
- **Real mobile descriptions** — The mobile budget sheet now writes the actual truncated description text into the description column instead of collapsing every row to a `…` placeholder.
- **Sheet title dropdown** — Folded the inline edit/view/download icons next to each sheet's title into a single per-view dropdown menu opened by tapping anywhere on the title.
- **No swipe row tint** — Dropped the soft-red background that tinted a row while its swipe action strip was open, since the slide animation already reads as a deliberate state change.
- **Pinned headers in modals** — Month markers and column headers now stay pinned while scrolling the transfers table, budget viewer, and account-history modal, matching the budget sheet.
- **robots.txt disallows preview** — `robots.txt` now explicitly disallows `/preview/` so crawlers skip the staging slot entirely instead of fetching it and finding the `noindex` meta tag.
- **Edit-recurring preview** — The **Edit recurring row** modal now lists every entry the chosen scope will touch and adds an **All rows in the series** scope that locks the amount field.
- **Reconciliation triage** — When an import fully covers a fiscal month, the reconciliation modal now groups orphan predictions by month with per-row and bulk move/keep/delete actions.
- **Gold coin app icon** — The app icon's dollar glyph now uses a subtle gold gradient with a soft warm halo so the home-screen tile reads like a burnished coin.
- **`not-allowed` cursor** — Show a `not-allowed` cursor when hovering the amount cell of history, transfer, or formula rows so it's clear they can't be edited inline.
- **Transfer edit affordance** — Edit a transfer with a pen icon in the Actions column or by left-swiping on mobile; the whole-row click target and account-name link are gone.
- **Modal clear buttons** — Inputs across every modal now carry an inline (×) clear button replacing tap-to-select-all, while cramped in-table cell editors keep their select-all.
- **Large-history table speedup** — Sped up the budget table on accounts with large histories by caching regexes, indexing company/type lookups, and reducing per-row and per-month passes.
- **Mobile fallback descriptions** — On mobile, bank-imported rows with a calculated fallback description now render the type name in its glyph colour, opening the editor with the raw bank text as placeholder.
- **Three render-hot algorithms** — Formula resolution, `coveredMonths`, and transaction search now use precomputed aggregates, hoisted bounds, and lowercase mirrors to scale on large datasets.
- **Three more hot algorithms** — Accounts balance, bank-import reconciliation, and the plain reconciliation matcher now use single-pass, `seriesId`-indexed, and binary-search approaches on large datasets.
- **Lazy synthesis & search index** — Editing on workspaces with hundreds of history entries is faster because synthesis now caches between cell edits and the search index builds lazily on demand.
- **Consistent field order** — Budget and history entry modals now show type, company, and description in the same order so switching between editing flows feels consistent.
- **History as bank statement** — The account history modal now reads as a bank statement, showing only the raw imported fields while keeping resolved metadata on the budget view.
- **Cross-sheet formula caching** — Formulas sharing a `sheet("…")` target now reuse a cached aggregate, and the import-rename reducer indexes history by id once instead of scanning per suggestion.
- **"Entry" vocabulary** — Renamed **Edit row** to **Edit entry**, its recurring variant to **Edit recurring entry**, and **Search transactions** to **Search entries** for consistent wording.
- **Rich desktop description popover** — The budget description cell now opens the same rich popover on desktop as on mobile, with company picker, description editor, and bank-memo line in one place.
- **Per-keystroke render speedup** — Sped up per-keystroke renders by skipping unchanged achievement predicates and removing `Intl.NumberFormat` from the history modal's search bar.
- **Three data-layer algorithm wins** — Cut per-keystroke and per-modal work via reference-identity short-circuiting in pattern-apply, LRU merchant-key eviction, and a slimmer single-account balance lookup.
- **Snappier large-workspace rendering** — Tightened three hot paths: cached match-rule lookups in history synthesis, a shared row sort for balance and display, and a pre-formatted viewer search index.
- **Wider search modal** — Widened the transaction search modal on desktop to match the Settings modal, giving search results a larger, steadier area to scan.
- **Transfer log modal** — The cross-account transfer log now opens in its own modal from the accounts title menu instead of sitting below the table, carrying the budget viewer's search bar.
- **Company-first dialogs** — Add and edit dialogs now show the company picker first, since picking a company can suggest a matching type, followed by type, tags, and description.

### Fixed

- **Description popover arrow seam** — The popover arrow now connects seamlessly to the panel below it, with the focus highlight extending across the arrow so it reads as one continuous shape.
- **Google Drive OAuth misroute** — Connecting Google Drive no longer lands on the Dropbox path when the redirect strips the `state` param, since the browser's PKCE verifier is now the source of truth.
- **Preview build OAuth redirect** — The preview's "Connect Dropbox" and "Connect Google Drive" buttons now round-trip back to `/preview/` instead of bouncing to the production root where auth silently aborted.
- **Add-row button behind tabs** — The add-row `+` button at the foot of the last month can now scroll clear of the floating sheet-tabs pill instead of hiding behind it.
- **iOS 26 Liquid Glass layout** — The budget now shows through Safari's translucent address bar and the bottom bar anchors correctly, fixing blank bands on empty sheets and the BottomBar position across browser and installed PWA.
- **Date picker first-tap dismiss** — The row date-cell picker no longer dismisses itself on the first tap, so selecting a day now works instead of being swallowed by the active-row coordinator.
- **Google Drive sign-in flow** — Drive now uses Google Identity Services' popup token client instead of the rejected PKCE redirect, so connecting no longer leaves the app or needs a registered redirect URI.
- **Description popover keyboard drift** — The description popover and its arrow now stay anchored to their row when the iOS keyboard opens instead of drifting toward the visible region's top edge.
- **Landscape phone sheet layout** — Phones in landscape now get the compact mobile layout with row-swipe actions, hidden header labels, and day-only date and icon-only type cells.
- **Cloud backups modal layout** — The cloud backups intro paragraph no longer squeezes into a narrow column beside "Back up now" on phones; the button drops below on narrow viewports.
- **Amount input overflow** — The amount input in the edit-row and edit-entry modals now shrinks with its column on narrow phones instead of stretching past the screen edge.
- **Cloud session reconnect dialog** — An expired cloud sync session now shows a dedicated Reconnect dialog with spinner, inline errors, and Retry, plus a Storage setting to disable the auto-pop-up.
- **Modal backdrop tap passthrough** — Tapping outside a modal now only dismisses it instead of also interacting with the sheet behind, and only a press starting on the backdrop closes it.
- **History cell tap freeze** — Tapping a description or type cell on an old row no longer freezes for seconds, since the sheet's render tree no longer rebuilds end-to-end on every focus or popover toggle.
- **History header row peek** — The account history modal no longer lets scrolled rows peek through above the sticky column header on iOS.
- **iOS reload focus pop** — Refreshing on iOS no longer reopens the last-edited amount cell with its value pre-selected, since numeric inputs now select on focus only after a real tap or keypress.
- **Cloud backend switch data loss** — Switching between cloud backends no longer risks blanking the destination, with backend-tagged caches, gated `Save now`, and a cloud glyph that stays visible.
- **Stale validator allowlist guard** — The validator now accepts every UI glyph, falls back to a default for unknowns, refuses to autosave the fresh-budget fallback over real data, and pauses shrinking saves over 5%.
- **Bulk-copy entry type** — Bulk-copying entries to other months now carries the entry type along, so you no longer have to re-pick it on every duplicated row.
- **"Back to budget" deploy slot** — "Back to budget" and the burger-menu Privacy and What's new links now respect the deploy slot instead of jumping from `/preview/` to the production root.
- **Editor tap dismiss focus** — Tapping outside an active sheet editor now only dismisses it, instead of also focusing the input or cell beneath the tap and popping the iOS keyboard.
- **Phantom sync conflict** — Cloud sync no longer surfaces a phantom "Sync conflict" when this device is the only editor, since saves now run one at a time with a single trailing save queued.
- **Recurring candidate rounding** — Promoting a recurring candidate now seeds the amount as a whole number rounded to the nearest integer instead of the full floating-point tail.
- **Distant-month render freeze** — Jumping to far-back search results or old history rows no longer freezes, since distant months render a height-matched placeholder instead of their full row tree.
- **History edit button alignment** — History rows now align their edit button with regular rows by reserving a disabled, struck-through trash slot showing history entries can't be deleted.
- **Balance update button centering** — The "Confirm balance update" button label is now centered to match the centered Cancel button below it.
- **History modal width** — The account history modal is now widened to match the sheet view modal so TYPE, AMOUNT, and BALANCE headers fit and month dividers span the full table.
- **Modal scroll chaining** — Scrolling inside an open modal no longer drags the page underneath; reaching the content edge now stays contained instead of chaining out to the page behind.
- **PWA fullscreen modal height** — Fullscreen modals on the installed PWA now extend through the full screen height on iOS 26, with a trimmed title bar and footer buttons above the home indicator.
- **Confirmation dialog labels** — Confirmation dialogs now center their action button labels to match Cancel, and the centered-card variant drops the status-bar safe-area room above the title.
- **Dropdown scroll chaining** — Touch-scrolling inside the burger menu and other dropdown popovers no longer drags the page underneath on mobile, since the panel now locks page scroll while open.
- **IndexedDB storage backend** — Budgets over ~5 MB no longer hit the localStorage quota, as the "This browser" backend now stores data in IndexedDB with automatic migration on next load.
- **Burger menu accent color** — Opening the page-header burger menu now highlights the trigger in the app's green accent instead of an off-palette purple.
- **Dropbox rate-limit handling** — Dropbox rate-limiting now turns the cloud icon orange and pauses auto-save briefly instead of a red "Sync failed", with saves resuming on their own.
- **Reload button cursor** — The Reload button in the "new build ready" update hint now shows the pointer cursor.
- **BottomBar row spacing** — Browser tabs now have breathing room between the last sheet row and the BottomBar, matching the installed-PWA layout.
- **Type chip alignment** — Type chips in the budget sheet are now left-aligned on desktop so glyphs line up across rows, while mobile keeps the centered glyph-only layout.
- **History balance clip** — The account history modal no longer clips the trailing "K" off the running-balance column, and tapping a description no longer pops a redundant read-only viewer.
- **Month title centering** — The month title now sits visually centered in its sticky band with matching padding, and the column headers tuck tighter so the header row's top edge stays visible.
- **iOS save scroll jump** — Editing a budget row on iOS no longer snaps the page to the top after Save, since the scroll lock now snapshots and restores the scroll position.
- **Sheet title spacing** — The sheet title at the top of each page now has matching breathing room above and below.
- **Untranslated UI strings** — Several UI strings now follow the language picker, including the loading screen, the sheet modal's account picker and disabled-type indicator, and the transfer modal's pickers.
- **"Keep mine" conflict resolution** — The "Keep mine" button in the sync conflict modal now actually pushes the local copy, instead of being blocked by the very conflict status it was meant to resolve.
- **Cleared description persistence** — Clearing a budget row's description now persists, since rows with any user-meaningful field keep their storage slot instead of being stripped as transient placeholders.
- **Popover close scroll jump** — Closing the description popover or any floating panel no longer yanks the page away from the edited row, since focus restoration now sets `preventScroll: true`.
- **History cleared description recurrence** — A cleared history-row description no longer comes back when a merchant hint or pattern fires, since the reducer now stamps an empty-string "explicit clear" marker.
- **Mobile modal footer offscreen** — Mobile modals like Edit account no longer push their Delete/Cancel/Save buttons off-screen; the footer now stays visible above the home indicator and keyboard.
- **Full-height sheets** — Every sheet now fills the full screen height instead of leaving a gap below sparse content until you switch sheets and back.

### Removed

- **JSON Schema page** — Removed the public JSON Schema page at `/schema` and the "Data schema" Settings link, since the persisted shape is documented in the TypeScript source.
- **"Find transfers" shortcut** — Dropped the "Find transfers" shortcut from the Accounts page header, while transfer-collapse still auto-opens after import and transfers remain manageable per-account.

## [0.1.0] - 2026-05-19

### Added

- "What's new" popup that surfaces release notes once after each upgrade,
  plus a browsable [Changelog page](/changelog) linked from the Settings
  footer next to Privacy policy and Data schema.
- Two-track deploy: the released app lives at `/`, the current `main` is
  previewed at `/preview/`. Visiting the preview never reads or writes
  your production data — local storage, the cloud-sync file, and the
  folder-handle database all carry a `preview` namespace.

### Changed

- The app now follows semantic versioning. The version string is shown
  in the "What's new" popup and on the Changelog page; the bundle's
  own `__APP_VERSION__` mirrors `package.json`.

